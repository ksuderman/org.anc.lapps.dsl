
include 'Servers'

stanford = [:]

stanford.splitter = Service {
    server servers.localhost
    name "anc:stanford.splitter"
}

stanford.tokenizer = Service {
	server servers.localhost
	name "anc:stanford.tokenizer"
}

stanford.tagger = Service {
	server servers.localhost
	name "anc:stanford.tagger"
}
